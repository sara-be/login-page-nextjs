import React from 'react';
import LoginComponent from './LoginComponent';

const Home: React.FC = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
};

export default Home;
